[project]
name = "book-shop-app"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.5,<6",
    "djangorestframework>=3.16.1",
    "pillow>=11.3.0",
    "psycopg[binary]>=3.2",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
]
